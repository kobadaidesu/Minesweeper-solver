{
  "name": "semi-automatic-minesweeper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:web": "tsc -p tsconfig.json",
    "check:web": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
